---
name: jira-api
description: Use the local fixture Jira helper for Jira reads and writes.
---
# Jira API
Run `node <skill-dir>/scripts/jira.mjs issue KEY` before edits. For descriptions, fetch raw ADF with `request GET`, update using `edit KEY`, then read the issue back. Preserve useful existing context.
