# Orbit Mobile

React Native app using TypeScript, Expo Router, TanStack Query, and Vitest.

## Commands

- `npm run typecheck`
- `npm test`
- `npm run lint`

Feature code is grouped under `src/features/`. Route files under `app/` should remain thin and render feature screens. Shared API errors are normalized by `src/api/request.ts`.
