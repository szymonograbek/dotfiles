# Orbit Mobile fixture

This reduced React Native workspace contains order-history and invoice-list features for the same signed-in account. It includes only the files needed to plan the order-history change described in `spec.md`.

```bash
npm run typecheck
npm test
```
